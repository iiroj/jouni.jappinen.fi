stages:
  - deploy

deploy:
  stage: deploy
  script:
    - yarn install
    - yarn run build
    - rm -fr /var/www/jouni.jappinen.fi/*
    - cp build/* /var/www/jouni.jappinen.fi/
  cache:
    paths:
      - node_modules
    key: "$CI_BUILD_REF"
    untracked: true
